# The Build

This where actual compilation starts, every chapter has separate documentation explainning the purpose of quirks while compiling packages listed in those chapters.


But first download the packages that needs to be installed.

The script 'download-packages.sh' which will download the `wget-list` and the packages from that list.


