# chapter 9: System Configuration 

`01/08/20206`

Almost everything is compiled and installed except the grub, will see what that leads to, or do I have to move to BLFS

Setup in done in following order - 
- Networking 
- device setup
- clock and keyboard
- scripts and configuration primer
- finally systemd configuration and usage.


LFS has listed alternatives/optional scripts in this chapter. Not every single configuratin is needed.

Skipping resolve conf as it is generated by systemd-resolved at boottime.

Most of the following scripts creats a file in `/etc/`
1. for ip qeuery the dchp, conf created using `01-dhcp.sh`
2. hostname, single line but created 'script' so not to forget `02-hosts.sh`
3. font for console created conf using `03-consoles.sh`
4. language conf using `04-locale.sh`
5. profile to load the local created in 4th script `05-profile.sh`
6. this is necessary for readline which in turn used by interactive term applicationsi `06-inputrc.sh`
7. used to find allowable shell `07-shells.sh`


A small configuration most of the things are handled by the systemd
next chapter is making LFS bootable (this is where I'll get the reference for the grub)
